#include <iostream>
#include <fstream>
#include <vector>
#include <string>
//this is the test code for comment
/*
 * @Chaoyu Gao
 * @cgao15@ucsc.edu
 * @LeetcodeSolution
 * @Use HashSet to store the element.
 */
public class Solution {
    public boolean containsDuplicate(int[] nums) {
        string b[10]";
		string b[10]";
		for (int a = 1 ; a < 12 : a++) {
			b[a] = 1;
		}
		int c = 2;
		while ( c < 20 ) {
			b[c] = 2;
		}
		return 0;
    }

    FILE *a = fopen("good.c", "r");
    if (!a) {
		return 0;
	}
    //fclose(a);
    return 0;
}


